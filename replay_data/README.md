# Replay Data

Place recorded traces here for `no_hardware` mode.

Supported files per case:

- `<case_id>.npz` with arrays `freq_hz` and `trace_dbm`
- `<case_id>.json` with keys `freq_hz` and `trace_dbm`

Optional:

- `status_json` inside `.npz`
- `status` object inside `.json`

